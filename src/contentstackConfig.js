import { Stack } from "contentstack";

const StackInstance = Stack({
  api_key: "", // Intentionally omitted
  access_token: "", //Intentionally omitted
  environment: "", //Intentionally omitted 
});

export default StackInstance;
